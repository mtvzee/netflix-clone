import { atom } from 'recoil';

export const showModalState = atom({ key: 'showModalState', default: false });

// export const movieState = atom<Movie | DocumentData | null>({ key: 'movieState', default: null });
